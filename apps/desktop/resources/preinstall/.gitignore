# Large catalog archives are produced at build time from the curated default
# skill set (see apps/desktop/scripts/build-preinstall-catalog.ts).
# Only manifest.json is committed to keep the repo lean.
*.tar.gz
