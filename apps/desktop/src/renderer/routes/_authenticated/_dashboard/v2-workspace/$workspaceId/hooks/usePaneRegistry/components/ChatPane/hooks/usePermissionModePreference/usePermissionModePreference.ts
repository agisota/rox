// Moved to a shared renderer location (see store.ts). Re-exported to keep this
// surface's existing import path stable.
export { usePermissionModePreference } from "renderer/components/Chat/ChatInterface/hooks/usePermissionModePreference/usePermissionModePreference";
